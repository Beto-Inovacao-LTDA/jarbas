# Tasks — Dados e Persistência

- [ ] 2.1 Implementar `Shortcut` (modelo + `toJson`/`fromJson`) em
      `settings_store.dart`
- [ ] 2.2 Implementar `SettingsStore` com os métodos de leitura/gravação
      de URL, token e atalhos
- [ ] 2.3 Implementar lista de atalhos padrão (`defaultShortcuts()`)

## Dependências

Depende de `00-arquitetura-base` (projeto criado, `shared_preferences` em
`pubspec.yaml`). Fornece a base para `02-integracao-home-assistant` e
`03-interface-modo-sob-demanda`.
